<div class="container">
  <div class="row">
    <ul class="list-unstyled">
      <% @flats.each do |flat| %>
        <li>
          <div class="col-xs-12 col-sm-4 text-center">
            <h3><%= link_to flat.title, flat_path(flat) %></h3>
            <p><%= flat.price_per_day %> € / day</p>
            <%= link_to flat_path(flat) do %>
              <img src="http://lorempixel.com/250/250/city/" alt="Random city picture">
            <% end %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</div>
