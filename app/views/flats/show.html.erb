<h1><%= @flat.title %></h1>
<p><%= @flat.address %></p>
<p><%= @flat.price_per_day %> € / day</p>
<p><%= @flat.housing_type %></p>
<p><%= @flat.capacity %> persons</p>
<p><%= @flat.description %></p>

<!-- TODO List all photos -->

<%= @flat.picture.flat_image.url(:medium).each do |img| %>
  <%= image_tag img %>
<% end %>

<%= link_to "Book this flat", new_flat_booking_path(@flat) %><br>

<%= link_to "Add a picture for the flat", new_flat_picture_path(@flat) %><br>
<%= link_to "Edit the flat", edit_flat_path(@flat) %><br>
<%= link_to "Back to all flats", flats_path %><br>
<%= link_to "Delete this flat", flat_path(@flat), method: :delete,
data:{ confirm: "Are you sure?" } %>